# Artefatos de ferramentas de teste/automação (não são conteúdo do projeto)
.playwright-mcp/

# Backups temporários gerados por rotinas (agenda, etc.) — nunca fonte de verdade
backups/

# Node
node_modules/

# Originais PNG dos renders Higgsfield (~84 MB) — a entrega é o .webp em
# site-producao/assets/img/. Mantidos localmente só para regerar em outro tamanho.
_fontes-img/

# Artefatos gerados pelas skills de auditoria (~67 MB de PNG e JSON).
# Reproduzíveis a qualquer momento; versionar só faz o repositório crescer.
site-producao/diagnosticos/
agenda-corrigida.png

# Credenciais — nunca versionar. Guardar em gerenciador de senhas, não em arquivo.
*.key
*.pem
.env
.env.*
*[Ss]enha*
*[Ss]ecret*
*credencia*
*[Aa]pi?[Kk]ey*
.devin/
graphify-out/

# Cache regenerado automaticamente
site-producao/relatorio_cache.json

# Log do servidor MCP local. Artefato de ferramenta, não conteúdo do projeto.
mcp-server-*.log

# Virtualenv morto: o Python 3.11 base sumiu da máquina em 26/07/2026 e deixou
# a árvore para trás. 348 MB em 16 mil arquivos, reprodutível. Fora do repositório.
automacao-yan-os/venv-morto-3.11/

# Venv avulso criado com uv (uv venv), sem nenhum script do repo apontando pra
# ele. Entrou no commit ebd8fdc por engano, 9073 arquivos e binários .exe.
# Reproduzível a qualquer momento, fora do repositório.
automacao-yan-os/venv-task/
