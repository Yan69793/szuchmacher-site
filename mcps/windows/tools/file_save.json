{
  "name": "file_save",
  "description": "💾 SAVE FILE TO DISK - The ONLY tool for saving documents. Automatically handles: Ctrl+S, Save As dialogs, filename entry, and overwrite prompts. ⚠️ DO NOT use keyboard_control(key='s', modifiers='ctrl') for saving - it CANNOT handle Save As dialogs and will get stuck! NOTE: English Windows only (detects 'Save As' dialog titles).\nWHEN TO USE: Any time you need to save a file in ANY application (Notepad, Word, VS Code, etc.). WHAT IT DOES: Sends Ctrl+S, waits for Save As dialog, enters filename, clicks Save, handles overwrite confirmation. WHY NOT KEYBOARD: keyboard_control sends Ctrl+S but CANNOT detect or interact with the Save As dialog that appears.\nReturns: Success status.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "windowHandle": {
        "description": "Window handle (from app or window_management 'find'). REQUIRED. Pass the APPLICATION window handle, not a dialog.",
        "type": "string"
      },
      "filePath": {
        "description": "Full path to save to (e.g., C:/Users/User/doc.txt). REQUIRED for new files. Forward/back slashes both work.",
        "type": [
          "string",
          "null"
        ],
        "default": null
      },
      "includeDiagnostics": {
        "description": "Include diagnostics (timing, query, elements scanned) in response. Default: false.",
        "type": "boolean",
        "default": false
      }
    },
    "required": [
      "windowHandle"
    ]
  }
}